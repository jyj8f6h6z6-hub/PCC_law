# 工程會電子法規彙編 v6.8（離線 / PWA 版）

本版以 v6.7 為基礎，保留原有法規、函釋、搜尋及智慧換行功能，新增離線使用能力。

## 離線內容
首次透過 HTTPS（例如 GitHub Pages）或 localhost / VS Code Live Server 成功開啟後，瀏覽器會快取：
- 網站介面（index.html、app.js、style.css）
- 145 筆工程會主管法規資料
- 3,666 筆 PRMS 函釋資料
- PWA manifest 與離線頁面

之後即使沒有網路，主要的法規閱讀、函釋閱讀及本機全文搜尋仍可使用。

## 手機安裝
1. 先在有網路時完整開啟網站一次，等待資料載入完成。
2. iPhone Safari：分享 → 加入主畫面。
3. Android Chrome：瀏覽器選單 → 安裝應用程式 / 加到主畫面。
4. 安裝完成後可先開啟一次，再切飛航模式測試。

## 電腦使用
建議使用 VS Code Live Server 或其他 localhost 靜態伺服器。Service Worker 不支援直接雙擊 index.html 的 file:// 模式，因此若直接雙擊檔案，離線 PWA 快取不會啟用。

## 注意
- 工程會、PRMS 等外部網站連結在完全離線時無法開啟。
- 本網站內已保存的法規與函釋內容不受影響。
- 若日後更新版本，Service Worker 的快取版本也應同步更新，避免裝置繼續使用舊檔。


## v6.9
- 全站搜尋的「主管法規」改為逐條／逐段命中，不再只列法規名稱。
- 搜尋結果顯示法規名稱、條／段位置及命中摘要。
- 點擊結果直接定位至該法規條文／段落，並標示搜尋字詞。
- 保留 v6.8 PWA 離線功能，更新快取版本。


## v6.10
- 全站搜尋的「主管法規」搜尋結果新增展開／收合功能。
- 搜尋範圍為「全部」時，主管法規結果預設收合，只顯示命中數量，避免函釋結果被推到過長頁面下方。
- 搜尋範圍指定為「工程會主管法規」時，結果預設展開。
- 展開後仍保留 v6.9 的逐條／逐段命中、摘要、關鍵字醒目標示及點擊定位功能。
- PWA Cache 更新至 v6.10。


## v6.12
- 全站搜尋的主管法規結果改為依「法規名稱」分組。
- 每一部法規可各自展開／收合命中條文或全文段落。
- 保留最上層「全部收合／全部展開」功能。
- PWA Cache 更新至 v6.12。


## v6.12
- 全站搜尋的主管法規結果依「法規名稱」分組，可逐部展開／收合，並保留全部展開／全部收合。
- 點擊主管法規命中位置時，不離開全站搜尋；桌機於右側顯示法規預覽並直接定位命中條／段。
- 預覽最底部提供「開啟完整法規頁面」連結，需要時再進入完整頁。

## v6.14
- 採購法逐條改為彙編式閱讀：本法條文下方直接顯示「相關施行細則」。
- 本法與施行細則對照採預先建立的 `data/act_rules_map.json`，目前只依施行細則條文中「明示引用本法第○條」建立，無明示依據者不強行配對。
- 原「政府採購法／政府採購法施行細則」獨立切換功能保留。
- 「引用／關聯主管法規」為 0 筆時不再顯示空白區塊；有資料時仍照常顯示。
- 保留 v6.12 主管法規搜尋分組、全部展開／收合、搜尋頁右側預覽及完整法規頁入口。
- PWA Cache 更新至 v6.14。


## v6.14
- 本法／施行細則對照改採使用者確認 Excel，不再由條文 Regex 自動推測。
- 採購法逐條改讀 json/政府採購法.json 與 json/政府採購法施行細則.json。
- data/laws.json 保留供 145 筆主管法規搜尋與瀏覽。


## v6.14 Fixed
- data/laws.json 已由 v4.1 修正版的 145 個獨立 JSON 重新產生。
- 採購法及施行細則專用 JSON 直接取自 v4.1 修正版母資料。
- 4.1 下載器資料夾不再放入部署包；它是上游母資料/下載工具，不是網站執行必要檔案。


## v6.15
- 採購法逐條的下拉選單改為 iOS 式「本法／施行細則」分段控制器。
- 畫面上的增訂條號統一顯示為「第5條之1」格式，內部識別值維持不變。
- PWA 快取版本更新。


## v6.16
- 全站搜尋的主管法規右側預覽：標題／關閉列固定於預覽頂端，內容捲動時仍可隨時關閉。
- 支援 Esc 鍵關閉右側預覽。
- 保留底部「開啟完整法規頁面」及命中條文定位／反黃功能。


## v6.17 試行版
- 函釋新增「對照法規」勾選區，預設全部不勾選。
- 函釋提及的不同法規可同時勾選並以獨立視窗顯示全文；同一法規多條合併於同一視窗並標示相關條文。
- 關閉法規視窗會同步取消勾選。
- 開啟函釋對照時會關閉全站主管法規搜尋的右側預覽。


## v6.17.1 試行修正版
- 修正函釋「對照法規」勾選後沒有顯示視窗的錯誤：v6.17 預覽函式誤呼叫不存在的 articleKey()。
- 改用既有條號正規化規則比對與定位。


## v6.17.2
- 函釋「對照法規」預覽改為只顯示該函釋實際提及的條文。
- 同一法規提及多條時，合併於同一視窗並只列出那些條文，不再載入前後無關條文。
- 主管法規搜尋的全文定位預覽維持原樣。
